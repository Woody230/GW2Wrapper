components:
  schemas:
    EmblemId:
      type: integer
      x-ktx-identifier: true

    Emblem:
      type: object
      required: [ id,layers ]
      x-ktx-identifiable:
        id: id
      properties:
        id:
          $ref: "#/components/schemas/EmblemId"
        layers:
          type: array
          description: The URLs representing the components of the emblem.
          items:
            $ref: "#/components/schemas/Url"
          